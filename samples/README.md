# Sample Certificates

These sample certificates are used for OCR layout testing and field extraction.

The MVP focuses on printed and typed text extraction. Handwritten or cursive fields are treated as review-needed fields unless OCR confidence is acceptable.